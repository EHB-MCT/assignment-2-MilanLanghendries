"""Handles data processing and aggregation."""
def load_data():
    """Mock function to load data."""
   # this is just placeholder data fow now
    return {
        "category": ["A", "B", "C", "D"],
        "values": [10, 20, 30, 40]
    }